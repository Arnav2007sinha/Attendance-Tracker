PORT=3000
DATABASE_URL=postgres://attendly:attendly@localhost:5432/attendly
JWT_SECRET=replace-with-a-long-random-production-secret
# For a local Flutter web preview; specify production origins explicitly in production.
ALLOWED_ORIGIN=http://localhost:*
# Only set this when every request arrives through your trusted campus gateway/reverse proxy.
TRUST_PROXY=false
